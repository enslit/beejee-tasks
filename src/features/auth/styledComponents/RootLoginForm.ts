import styled from 'styled-components';
import { Paper } from '@mui/material';

export const RootLogin = styled(Paper)`
  width: 100%;
  max-width: 500px;
  padding: 16px;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
